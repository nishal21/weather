# Live weather (default). Use mock only for offline UI tests.
# SERVER ONLY — never prefix secrets with NEXT_PUBLIC_
WEATHER_DATA_SOURCE=open-meteo

# Optional server-only keys (never exposed to the browser)
# BIGDATACLOUD_API_KEY=
# MYMEMORY_CONTACT_EMAIL=
# IMD_API_KEY=

# Public site URL only (safe for client bundle — not a secret)
NEXT_PUBLIC_SITE_URL=https://your-domain.com
